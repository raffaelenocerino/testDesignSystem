on:
    repository_dispatch:
        types: [update-tokens]
        push:
            branches:
                - main
jobs:
  save_in_repo:
    runs-on: ubuntu-latest
    steps:
      - env:
            payload:    ${{ github.event.client_payload }}
        run:
            echo "${{env.payload}}" > designTokens.json
            git add designTokens.json
            git commit -m "New design token"
            git push
